import "use cache";

/**
 * サンプルデータの一覧を取得する
 *
 * @remarks
 * スキャフォールド用のスタブ実装。
 * 実際の Feature では DB からの SELECT クエリに置き換える。
 *
 * @returns サンプルデータの配列（現在は空配列）
 *
 * @example
 * ```tsx
 * // Server Component から呼び出す
 * const samples = await getSamples();
 * ```
 *
 * @alpha
 */
export async function getSamples() {
  return [];
}
